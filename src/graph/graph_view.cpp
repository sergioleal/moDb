#include "modb/graph/graph_view.hpp"

#include <string_view>

// Unidade de tradução da Fase 12B: a API tipada vive no header; este .cpp
// ancora a biblioteca e documenta o ponto de extensão para 12C (travessias).

namespace modb::graph {

namespace {

[[maybe_unused]] constexpr std::string_view k_phase = "12B";

} // namespace

} // namespace modb::graph
