# Build trees
/build/
/build-linux/
/cmake-build-*/
/out/

# IDEs and editors
/.idea/
/.vscode/
*.user

# Generated files
compile_commands.json
Testing/

# Runtime database files
*.modb
*.modb-wal
*.modb.wal
*.db
*.db-wal

# Benchmark campaign outputs (kept local; selected baselines may be committed under docs/)
/benchmark-results/

# Load test raw campaign outputs (kept local; the historical series lives in
# /load-history/, which IS versioned — see docs/PLANO_TESTES_DE_CARGA.md)
/load-results/

# Coverage report outputs (gcovr/gcov)
/coverage-results/

